{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for the Media Player main window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
